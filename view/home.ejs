<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Hi <%= user.name %> !!!</h1>
    <form action="/url/create" method="post">
        <input hidden type="text" name="createdby" value="<%= user._id %>">
        <label for="">Original-URL</label>
        <input type="text" name="originalURL">
        <button type="submit">Create</button>    
    </form>
    <p>
        <table>
            <thead>
                <th>Serial Number</th>
                <th>Original URL</th>
                <th>Short URL</th>
                <th>Clicks</th>
            </thead>
            <% for( let index = 0; index < urls.length; index++ ) { %>
                <tr>
                    <td><%= index+1 %></td>
                    <td><%= urls[index].originalURL %></td>
                    <td><%= urls[index].shortURL%></td>
                    <td><%= urls[index].visits.length %></td>
                </tr>
            <% } %>
        </table>
    </p>
</body>
</html>